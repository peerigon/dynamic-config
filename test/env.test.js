"use strict";

import test from "ava";
import envPlugin from "../plugins/extend/env"; // eslint-disable-line no-unused-vars

test("Should read config keys from process.env", () => {
    // Missing tests
});
