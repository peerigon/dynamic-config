"use strict";

import test from "ava";
import filePlugin from "../plugins/extend/file"; // eslint-disable-line no-unused-vars

test("Should read config keys from a local file", () => {
    // Missing tests
});
